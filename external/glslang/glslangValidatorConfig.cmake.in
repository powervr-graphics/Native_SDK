include("${CMAKE_CURRENT_LIST_DIR}/glslangValidatorTargets.cmake")

if(TARGET glslang-standalone AND NOT TARGET glslangValidator)
    add_executable(glslangValidator ALIAS glslang-standalone)
endif()
